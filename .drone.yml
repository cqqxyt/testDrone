kind: pipeline
name: api
steps:
  # api unit test
  - name: test1
    image: node:16
    depends_on: [clone]
    commands:
      - npm install
      - npm run test
    when:
      branch: master
      event: push
